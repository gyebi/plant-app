# Plant App Workspace

This workspace contains a React learning project for building a simple plant app.

## Project

The app lives in [`plant-project`](./plant-project).

To run it:

```bash
cd plant-project
npm install
npm run dev
```

Use this workspace as the outer folder for notes, experiments, and future React projects if you want to keep the app itself clean.
